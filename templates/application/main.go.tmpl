package main

import (
	"net/http"

	rice "github.com/GeertJohan/go.rice"
	"github.com/gorilla/mux"
	"github.com/improbable-eng/grpc-web/go/grpcweb"
	"google.golang.org/grpc"
	log "github.com/sirupsen/logrus"
	"{{.ImportPath}}/app"
	"{{.ImportPath}}/util/web"
	{{- if .Recipe.Bread.Generate}}
	"{{.ImportPath}}/services/bread"
	{{- end}}
)

//go:generate rice embed-go

{{if not .Recipe.Bootstrap.NoVersion}}
// Versioning info
var (
	appVersion = "n/a"
	appCommit  = "n/a"
	appBuilt   = "n/a"
)
{{end}}

func main() {
	{{- if not .Recipe.Bootstrap.NoVersion}}
	version := flag.Bool("v", false, "prints current app version")
	flag.Parse()
	if *version {
		fmt.Printf("Version : %v \nCommit : %v\nBuilt: %v\n", appVersion, appCommit, appBuilt)
		os.Exit(0)
	}
	{{end}}

	config := app.Bootstrap()
	models.Init(config.DB)
	
	{{if .Recipe.Bootstrap.Assets}}
	app.Assets = rice.MustFindBox("assets")
	{{end}}

	{{if not .Recipe.Bootstrap.NoGRPCWeb}}
	g := grpc.NewServer()
	{{- if .Recipe.Bread.Generate}}
	bread.RegisterBreadServer(g, &bread.Service{})
	{{- end}}
	ws := grpcweb.WrapServer(g)
	var router *mux.Router
	if app.Env == app.EnvironmentDev {
		router = newDevRouter({{if .Recipe.Bread.Generate}}ws{{else}}nil{{end}})
	} else {
		router = newProdRouter(ws)
	}
	{{end}}
	
	{{if not .Recipe.Bootstrap.NoGRPCWire}}
	go func() {
		l, err := net.Listen("tcp", ":"+config.GRPCPort)
		if err != nil {
			log.Fatalf("error starting tcp listener: %v", err)
		}
		defer l.Close()

		g := grpc.NewServer()
		if err := g.Serve(l); err != nil {
			log.Fatalf("error serving grpc: %v", err)
		}
	}()
	{{end}}

	srv := &http.Server{
		Handler:      router,
		Addr:         ":" + config.HTTPPort,
		ReadTimeout:  5 * time.Second,
		WriteTimeout: 10 * time.Second,
		IdleTimeout:  15 * time.Second,
	}

	log.Println("Listening on http://127.0.0.1:" + config.HTTPPort)
	if err := srv.ListenAndServe(); err != nil {
		log.Fatalf("error starting http server: %v", err)
	}
}
