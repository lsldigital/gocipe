<template>
    <mj-section :background-url="content.value.background_url" vertical-align="middle" background-size="cover" full-width="full-width" background-repeat="no-repeat">
        <mj-column width="100%" vertical-align="middle">
            <mj-text align="center" font-size="14px" color="#45474e" padding-bottom="10px" padding-top="45"><span style="font-size: 30px; line-height: 30px;">{{ "{{content.value.title}}" }}</span><br><br>{{ "{{content.value.description}}" }}</mj-text>
            <mj-button align="center" background-color="#e85034" color="#fff" border-radius="24px" :href="content.value.button_url" font-family="Ubuntu, Helvetica, Arial, sans-serif, Helvetica, Arial, sans-serif" padding-bottom="45px" padding-top="10">{{ "{{content.value.button_text}}" }}</mj-button>
        </mj-column>
    </mj-section>
</template>
<script>
    export default {
        props: [
            'content'
        ]
    }
</script>
