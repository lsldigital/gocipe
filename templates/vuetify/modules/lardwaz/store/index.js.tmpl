import actions from "./actions";
import getters from "./getters";
import mutations from "./mutations";
import PageComponents from "@/../modules/lardwaz-config/page-components.js";

const state = {
  blocks: [],
  toolbox: PageComponents,
  viewport: "desktop"
};

const namespaced = true;

export default {
  namespaced,
  state,
  actions,
  getters,
  mutations
};
