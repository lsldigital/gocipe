function getBlocks(state) {
  return state.blocks;
}

function getToolbox(state) {
  return state.toolbox;
}

let getViewport = (state) => {
  return state.viewport
}

export default {
  getBlocks,
  getToolbox,
  getViewport
};
