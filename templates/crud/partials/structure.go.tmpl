
// {{.Name}} {{.Description}}
type {{.Name}} struct {
	ID        *{{pkeyPropertyType .PrimaryKey}} `json:"id"`
	{{- range .Fields}}
	{{.Name}} {{.Type}} `json:"{{.Serialized}}"`
	{{- end}}
}

// New returns an instance of {{.Name}}
func New{{.Name}}() {{.Name}} {
	return {{.Name}}{
		ID: new({{pkeyPropertyType .PrimaryKey}}),
		{{- range .Initialization}}
		{{.Name}}: {{.Type}},
		{{- end}}
	}
}

// {{.Name}}Repository encapsulates operations that may be performed on the entity {{.Name}}
type {{.Name}}Repository struct {
	db *sql.DB
}
