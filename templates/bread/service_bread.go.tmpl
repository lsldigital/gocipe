// This file can be modified to accommodate other variables to be injected in
// BREAD service

package bread

import "database/sql"

var (
	_db *sql.DB
)

// Inject injects stuffs in bread
func Inject(db *sql.DB)  {
	_db = db
}
