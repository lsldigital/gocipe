package crud

import (
	"testing"
)

func TestFactory(t *testing.T) {
	// var args []string
	// args[0] = "crud"
	// args[1] = "Person"
	// args[2] = "-g"
	// output, err := factory(args)
	// expected := ``

	// if err != nil {
	// 	t.Errorf("Got error: %s", err)
	// } else if 1 != 2 {
	// 	t.Errorf(`Output not as expected.`)
	// 	t.Log(output, expected)
	// }
}
